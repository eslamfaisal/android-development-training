import 'package:flutter/material.dart';

class AlertWarningDialog extends StatelessWidget {
  const AlertWarningDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
